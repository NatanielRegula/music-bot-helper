{
	"scripts": {
		"build": "zpl build",
		"init": "zpl init",
		"watch": "while inotifywait -e close_write src/ZoxMusicBotHelper/*; do zpl build ZoxMusicBotHelper; done"
	},
	"devDependencies": {
		"zerespluginlibrary": "^2.0.6"
	}
}